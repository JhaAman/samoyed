import React, { ReactElement } from "react";

interface Props {}

export default function AnswerPanel({}: Props): ReactElement {
  return <div className="flex flex-col items-center w-1/2 m-4 bg-base"></div>;
}
